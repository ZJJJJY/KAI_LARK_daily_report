[project]
name = "kai-lark-daily-report"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn>=0.30.0",
  "httpx>=0.27.0",
  "cryptography>=42.0.0",
]

[dependency-groups]
dev = [
  "pytest>=8.0.0",
]
